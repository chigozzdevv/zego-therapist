# ZEGO Configuration
# Get these from ZEGO Console (https://console.zegocloud.com):
# 1. Create a ZEGOCLOUD account and sign in
# 2. Create a new project in the Admin Console
# 3. Click on the project you created to open project overview page with config details
# 4. Copy the App ID (unique identifier for your project)
# 5. Click the eye button next to ServerSecret to view and copy it
# Note: Keep ServerSecret strictly confidential for security

ZEGO_APP_ID=your_zego_app_id_here
ZEGO_SERVER_SECRET=your_zego_server_secret_here
ZEGO_API_BASE_URL=https://aigc-aiagent-api.zegotech.cn

# DashScope API Configuration (for Qwen LLM and CozyVoice TTS)
# The DASHSCOPE_API_KEY is used as the LLM api_key in the Zego Agent config
# You can use "zego_test" key if you're within the free 2-week trial period
#
# To get your DASHSCOPE_API_KEY:
# 1. Sign up on Alibaba Cloud (https://www.alibabacloud.com/)
# 2. Go to Model Studio in the dashboard (https://www.alibabacloud.com/en/product/modelstudio)
# 3. Navigate to the API-Key page and click "Create API Key"
# 4. Select "Default workspace" for Workspace (recommended)
# 5. Copy the generated API key
#
# Models used:
# - Qwen: Language model for chat/text generation
# - CozyVoice: Text-to-speech provider
DASHSCOPE_API_KEY=sk-your_dashscope_api_key_here

# Server Configuration
PORT=8080
NODE_ENV=development
# Update this to your deployed server URL (e.g., Render, Vercel, etc.)
# For local development, use: http://localhost:8080
SERVER_URL=http://localhost:8080